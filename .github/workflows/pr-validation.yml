name: PR Validation

on:
  pull_request:
    branches: [develop, staging, main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "1"
  NUGET_PACKAGES: ${{ github.workspace }}/.ci-nuget/packages

jobs:
  dependency-packages:
    name: Build exact public-source dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      dependency-artifact-digest: ${{ steps.upload-dependencies.outputs.artifact-digest }}
    steps:
      - name: Checkout OrderService source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Checkout exact MessagingContracts source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: MALIEV-Co-Ltd/Maliev.MessagingContracts
          ref: 0bcd4c704d842211c5ff9bd6b9c4b3aacfcbd8e7
          path: .ci-sources/Maliev.MessagingContracts
          persist-credentials: false

      - name: Checkout exact ServiceDefaults source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: MALIEV-Co-Ltd/Maliev.Aspire
          ref: 7121d57705fc1eff6c7ebb6a69e33e9c26ebfccc
          path: .ci-sources/Maliev.Aspire
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          dotnet-version: 10.0.x

      - name: Build exact dependency packages
        run: bash scripts/prepare-order-ci-packages.sh

      - name: Upload exact dependency packages
        id: upload-dependencies
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: order-ci-packages
          path: |
            .ci-packages/*.nupkg
            .ci-packages/SHA256SUMS.txt
          if-no-files-found: error
          include-hidden-files: true
          overwrite: true
          retention-days: 1

      - name: Validate dependency artifact digest
        shell: bash
        env:
          ARTIFACT_DIGEST: ${{ steps.upload-dependencies.outputs.artifact-digest }}
        run: |
          set -euo pipefail
          test -n "$ARTIFACT_DIGEST"
          [[ "$ARTIFACT_DIGEST" == sha256:* ]]

  build-and-test:
    name: Release build and test
    needs: dependency-packages
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Set up .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          dotnet-version: 10.0.x

      - name: Download exact dependency packages
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: order-ci-packages
          path: .ci-packages

      - name: Verify exact dependency packages
        shell: bash
        env:
          ARTIFACT_DIGEST: ${{ needs.dependency-packages.outputs.dependency-artifact-digest }}
        run: |
          set -euo pipefail
          test -n "$ARTIFACT_DIGEST"
          (cd .ci-packages && sha256sum --check SHA256SUMS.txt)

      - name: Restore
        run: >-
          dotnet restore Maliev.OrderService.slnx
          --configfile NuGet.PRValidation.Config
          -p:GITHUB_ACTIONS=true
          -p:ServiceDefaultsVersion=1.0.81-alpha
          -p:MessagingContractsVersion=1.0.91-alpha

      - name: Record resolved package graph
        run: dotnet list Maliev.OrderService.slnx package --include-transitive --no-restore

      - name: Build
        run: dotnet build Maliev.OrderService.slnx --configuration Release --no-restore -p:GITHUB_ACTIONS=true -p:ServiceDefaultsVersion=1.0.81-alpha -p:MessagingContractsVersion=1.0.91-alpha

      - name: Test
        run: dotnet test Maliev.OrderService.slnx --configuration Release --no-build --verbosity normal -p:GITHUB_ACTIONS=true -p:ServiceDefaultsVersion=1.0.81-alpha -p:MessagingContractsVersion=1.0.91-alpha

  container:
    name: Build and scan production image
    needs: dependency-packages
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      IMAGE_TAG: maliev-order-service:pr-validation-${{ github.run_id }}
    steps:
      - name: Checkout source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Download exact dependency packages
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: order-ci-packages
          path: .ci-packages

      - name: Verify exact dependency packages
        shell: bash
        env:
          ARTIFACT_DIGEST: ${{ needs.dependency-packages.outputs.dependency-artifact-digest }}
        run: |
          set -euo pipefail
          test -n "$ARTIFACT_DIGEST"
          (cd .ci-packages && sha256sum --check SHA256SUMS.txt)

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Build local production image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Maliev.OrderService.Api/Dockerfile
          load: true
          push: false
          tags: ${{ env.IMAGE_TAG }}
          build-args: |
            dependency_restore_stage=restore-local

      - name: Validate runtime image metadata
        shell: bash
        run: |
          set -euo pipefail
          metadata="$(docker image inspect "$IMAGE_TAG")"
          test "$(jq -r '.[0].Config.User' <<< "$metadata")" = "app"
          test "$(jq -c '.[0].Config.Entrypoint' <<< "$metadata")" = '["dotnet","Maliev.OrderService.Api.dll"]'
          test "$(jq -r '.[0].Config.ExposedPorts | has("8080/tcp")' <<< "$metadata")" = "true"

      - name: Smoke test production image
        shell: bash
        run: |
          set -euo pipefail
          network_name="maliev-order-smoke-${GITHUB_RUN_ID}"
          database_name="maliev-order-db-${GITHUB_RUN_ID}"
          service_name="maliev-order-api-${GITHUB_RUN_ID}"

          # shellcheck disable=SC2317 # Invoked indirectly by the EXIT trap.
          cleanup() {
            docker rm --force "$service_name" "$database_name" >/dev/null 2>&1 || true
            docker network rm "$network_name" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          docker network create "$network_name" >/dev/null
          docker run --detach \
            --name "$database_name" \
            --network "$network_name" \
            --network-alias postgres \
            --env POSTGRES_DB=orders \
            --env POSTGRES_USER=postgres \
            --env POSTGRES_PASSWORD=postgres \
            postgres:18-alpine >/dev/null

          for attempt in {1..30}; do
            if docker exec "$database_name" pg_isready --username postgres --dbname orders >/dev/null 2>&1; then
              break
            fi
            if [ "$attempt" -eq 30 ]; then
              docker logs "$database_name"
              exit 1
            fi
            sleep 1
          done

          docker run --detach \
            --name "$service_name" \
            --network "$network_name" \
            --publish "127.0.0.1:18080:8080" \
            --env ASPNETCORE_ENVIRONMENT=Testing \
            --env Cache__RedisEnabled=false \
            --env CORS__AllowedOrigins__0=http://localhost \
            --env MassTransit__UseInMemory=true \
            --env ConnectionStrings__OrderDbContext="Host=postgres;Port=5432;Database=orders;Username=postgres;Password=postgres" \
            "$IMAGE_TAG" >/dev/null

          for attempt in {1..60}; do
            if curl --fail --silent --show-error "http://127.0.0.1:18080/order/liveness" >/dev/null 2>&1; then
              exit 0
            fi

            if [ "$(docker inspect --format '{{.State.Running}}' "$service_name")" != "true" ]; then
              docker logs "$service_name"
              exit 1
            fi

            sleep 1
          done

          docker logs "$service_name"
          exit 1

      - name: Generate image SBOM
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.IMAGE_TAG }}
          format: cyclonedx
          output: trivy-sbom.cdx.json
          scanners: vuln
          exit-code: "0"

      - name: Validate SBOM output
        run: test -s trivy-sbom.cdx.json

      - name: Scan image for high-severity vulnerabilities
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.IMAGE_TAG }}
          format: table
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "1"
