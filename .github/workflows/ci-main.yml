name: Validate - Main

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: order-validate-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: ./.github/workflows/_validate.yml
